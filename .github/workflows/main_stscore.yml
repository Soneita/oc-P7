name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0
        with:
          ssh-private-key: ${{ secrets.oc }}  # Utilisez le secret repository 'oc'

      - name: Deploy to EC2
        run: |
          scp -i /dev/stdin /Users/soneitaraherimalala/Desktop/oc-P7/source/P7_script.py ubuntu@ip-172-31-39-83:/home/ubuntu/oc-P7

